import React from "react";

const a = 20;
const b = 20;
const sum = a + b;

function App() {
  return (
    <>
    <h1> 20 + 20  = {sum} </h1>,
    <h1>{Math.abs(3/5)}</h1>.
    <h1>{Math.round(5/3)}</h1>.
    </>
  );
}

export default App;
